# vue-tailwind-shop

Monorepo setup:

- App: `apps/web`
- Stack: Vue 3 + TypeScript + Vite + Vue Router

Run it:

```bash
pnpm install
pnpm dev
```

Routes:

- `#/home`
- `#/products`
- `#/product`
- `#/checkout`
